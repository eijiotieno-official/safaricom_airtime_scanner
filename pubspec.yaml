name: safaricom_airtime_scanner
description: "Scan your Safaricom scratch card"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ussd_advanced: ^1.1.0
  google_ml_kit: ^0.16.3
  image_picker: ^1.0.7
  camera: ^0.10.5+9
  clipboard: ^0.1.3
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
